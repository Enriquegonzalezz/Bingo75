# ============================================================================
# 🔴 JHONATHAN: VARIABLES DE ENTORNO PARA LA BASE DE DATOS
# ============================================================================
# Copia este archivo como .env.local en la raíz del proyecto
# Luego configura las variables según tu base de datos
# ============================================================================

# ============================================================================
# OPCIÓN 1: PostgreSQL con Prisma
# ============================================================================
DATABASE_URL="postgresql://usuario:password@localhost:5432/bingo75?schema=public"

# O si usas un servicio en la nube:
# DATABASE_URL="postgresql://usuario:password@tu-host.com:5432/bingo75?schema=public"

# Ejemplos de servicios populares:
# Supabase: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Neon: postgresql://[user]:[password]@[endpoint].neon.tech/[dbname]
# Railway: postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/railway

# ============================================================================
# OPCIÓN 2: PostgreSQL con pg (pool directo)
# ============================================================================
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=postgres
# DATABASE_PASSWORD=tu_password
# DATABASE_NAME=bingo75

# ============================================================================
# OPCIÓN 3: MySQL
# ============================================================================
# DATABASE_URL="mysql://usuario:password@localhost:3306/bingo75"

# O con variables separadas:
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_USER=root
# DATABASE_PASSWORD=tu_password
# DATABASE_NAME=bingo75

# ============================================================================
# OPCIÓN 4: SQLite (solo para desarrollo local)
# ============================================================================
# DATABASE_URL="file:./dev.db"

# ============================================================================
# INSTRUCCIONES
# ============================================================================
# 1. Copia este archivo como .env.local
# 2. Descomenta y configura las variables según tu base de datos
# 3. NO subas el archivo .env.local a Git (ya está en .gitignore)
# 4. Reinicia el servidor de desarrollo después de cambiar variables de entorno
# ============================================================================
